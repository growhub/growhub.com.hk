# growhub-ai — Worker that runs the prototype-planner inference on Workers AI.
# The Pages Function /api/prototype calls this via a Service binding (AISVC).
# Deploy:  cd worker/ai && npx wrangler deploy
name = "growhub-ai"
main = "src/index.ts"
compatibility_date = "2024-11-01"
account_id = "de779d074dc4928610876987e009b20c"

[ai]
binding = "AI"
